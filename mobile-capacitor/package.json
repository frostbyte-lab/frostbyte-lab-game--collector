{
  "name": "gc-pro-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for Game Collector Pro (loads live Worker URL)",
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
