import React, { useMemo, useState } from 'react';
import {
  ActivityIndicator,
  Alert,
  FlatList,
  Image,
  KeyboardAvoidingView,
  Modal,
  Platform,
  Pressable,
  ScrollView,
  StyleSheet,
  Text,
  TextInput,
  View,
} from 'react-native';
import * as DocumentPicker from 'expo-document-picker';
import * as FileSystem from 'expo-file-system/legacy';
import * as Haptics from 'expo-haptics';
import { Audio, ResizeMode, Video } from 'expo-av';
import { Ionicons } from '@expo/vector-icons';
import { WebView } from 'react-native-webview';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import JSZip from 'jszip';
import { useColors } from '@/hooks/useColors';
import { DEFAULT_PLUGIN_STATE, PLUGINS } from '@/lib/plugins';

type Kind = 'web' | 'image' | 'audio' | 'video' | 'code' | 'other';
type Entry = { name: string; size: number; isDirectory: boolean; extension: string; kind: Kind; zipObject?: JSZip.JSZipObject };
type Token = { value: string; tone: 'plain' | 'keyword' | 'string' | 'comment' | 'number' | 'tag' | 'property' | 'operator' };

const imageTypes = ['png', 'jpg', 'jpeg', 'gif', 'webp', 'svg'];
const audioTypes = ['mp3', 'wav', 'ogg', 'm4a', 'aac'];
const videoTypes = ['mp4', 'mov', 'webm', 'mkv', 'avi'];
const codeTypes = ['css', 'js', 'jsx', 'ts', 'tsx', 'py', 'py3', 'json', 'md', 'txt', 'xml', 'yml', 'yaml'];
const keywords = new Set('as async await break case catch class const continue def default delete else export extends false finally for from function if import in interface let new null of return static super switch this throw true try type typeof var void while with yield public private protected extends implements package'.split(' '));

function kindFor(ext: string): Kind { if (ext === 'html' || ext === 'htm') return 'web'; if (imageTypes.includes(ext)) return 'image'; if (audioTypes.includes(ext)) return 'audio'; if (videoTypes.includes(ext)) return 'video'; if (codeTypes.includes(ext)) return 'code'; return 'other'; }
function formatBytes(bytes: number) { if (!bytes) return '0 B'; if (bytes < 1024) return bytes + ' B'; if (bytes < 1048576) return (bytes / 1024).toFixed(1) + ' KB'; return (bytes / 1048576).toFixed(1) + ' MB'; }
function iconFor(kind: Kind): keyof typeof Ionicons.glyphMap { return ({ web: 'globe-outline', image: 'image-outline', audio: 'musical-notes-outline', video: 'videocam-outline', code: 'code-slash-outline', other: 'document-outline' } as const)[kind]; }

function tokenizeLine(line: string, extension: string): Token[] {
  const tokens: Token[] = [];
  const pattern = /\/\/.*$|#.*$|\/\*[\s\S]*?\*\/|<!--[\s\S]*?-->|`[^`]*`|'[^']*'|"[^"]*"|\b\d+(?:\.\d+)?\b|[A-Za-z_$][\w$-]*|===|!==|=>|==|!=|<=|>=|&&|\|\||[{}()[\].,;:+*/%=<>!-]/g;
  let cursor = 0; let match: RegExpExecArray | null;
  while ((match = pattern.exec(line))) {
    if (match.index > cursor) tokens.push({ value: line.slice(cursor, match.index), tone: 'plain' });
    const value = match[0]; const lower = value.toLowerCase();
    let tone: Token['tone'] = 'plain';
    if (value.startsWith('//') || value.startsWith('#') || value.startsWith('/*') || value.startsWith('<!--')) tone = 'comment';
    else if (/^[`'\"]/.test(value)) tone = 'string';
    else if (/^\d/.test(value)) tone = 'number';
    else if (keywords.has(value)) tone = 'keyword';
    else if (extension === 'json' && /^[A-Za-z_$]/.test(value) && line.slice(match.index + value.length).trimStart().startsWith(':')) tone = 'property';
    else if (extension === 'html' || extension === 'htm' || extension === 'xml') tone = lower === 'html' || lower === 'body' || lower === 'script' || lower === 'div' || lower === 'head' ? 'tag' : 'plain';
    else if (/^[{}()[\].,;:+*/%=<>!-]/.test(value)) tone = 'operator';
    tokens.push({ value, tone }); cursor = match.index + value.length;
  }
  if (cursor < line.length) tokens.push({ value: line.slice(cursor), tone: 'plain' });
  return tokens.length ? tokens : [{ value: ' ', tone: 'plain' }];
}

function HighlightedCode({ text, extension, colors }: { text: string; extension: string; colors: ReturnType<typeof useColors> }) {
  const toneColors: Record<Token['tone'], string> = { plain: colors.foreground, keyword: '#d78cff', string: '#8fe388', comment: '#718096', number: '#ffbd6b', tag: '#5ee1c0', property: '#82cfff', operator: '#ff8fab' };
  return <View accessible accessibilityLabel="Syntax highlighted code">{text.split('\n').map((line, lineIndex) => <Text key={lineIndex} selectable style={[styles.code, { color: colors.foreground }]}><Text style={styles.lineNumber}>{String(lineIndex + 1).padStart(3, ' ')}  </Text>{tokenizeLine(line, extension).map((token, tokenIndex) => <Text key={tokenIndex} style={{ color: toneColors[token.tone] }}>{token.value}</Text>)}</Text>)}</View>;
}

function analyzeCode(text: string, entry: Entry) {
  const findings: string[] = [];
  if (!text.trim()) findings.push('WARN: file kosong.');
  if (/https?:\/\//i.test(text)) findings.push('INFO: ditemukan URL eksternal; periksa kebutuhan offline.');
  if (/console\.(log|error|warn)\s*\(/.test(text)) findings.push('INFO: ditemukan logging runtime.');
  if ((text.match(/[{}()[\]]/g) || []).length % 2 !== 0) findings.push('WARN: jumlah delimiter tidak seimbang; periksa sintaks.');
  if (/TODO|FIXME|XXX/.test(text)) findings.push('WARN: masih ada penanda pekerjaan TODO/FIXME.');
  if (!findings.length) findings.push('PASS: audit lokal tidak menemukan pola masalah umum pada ' + entry.name + '.');
  return findings;
}

export default function ZipPreviewScreen() {
  const colors = useColors(); const [archiveName, setArchiveName] = useState(''); const [entries, setEntries] = useState<Entry[]>([]); const [selected, setSelected] = useState<Entry | null>(null);
  const [previewText, setPreviewText] = useState(''); const [previewUri, setPreviewUri] = useState(''); const [busy, setBusy] = useState(false); const [logs, setLogs] = useState<string[]>([]); const [fullPreview, setFullPreview] = useState(false); const [editing, setEditing] = useState(false); const [draftText, setDraftText] = useState(''); const [editedContent, setEditedContent] = useState<Record<string, string>>({}); const [pluginPanel, setPluginPanel] = useState(false); const [pluginState, setPluginState] = useState<Record<string, boolean>>(DEFAULT_PLUGIN_STATE);
  const log = (message: string) => setLogs(current => [(new Date()).toLocaleTimeString() + '  ' + message, ...current].slice(0, 80));
  const openArchive = async () => {
    await Haptics.selectionAsync(); setBusy(true); setLogs([]); setEntries([]); setSelected(null); setPreviewText(''); setPreviewUri(''); setEditedContent({});
    try {
      const result = await DocumentPicker.getDocumentAsync({ type: ['application/zip', 'application/x-zip-compressed', '*/*'], copyToCacheDirectory: true, multiple: false });
      if (result.canceled) return;
      const asset = result.assets[0]; const size = asset.size ?? 0; setArchiveName(asset.name); log('Membuka ' + asset.name + ' (' + formatBytes(size) + ')');
      let zip: JSZip;
      try { const response = await fetch(asset.uri); zip = await JSZip.loadAsync(await response.arrayBuffer(), { checkCRC32: false, createFolders: true }); }
      catch (binaryError) { log('Binary reader gagal, mencoba reader kompatibilitas: ' + (binaryError instanceof Error ? binaryError.message : 'unknown')); const base64 = await FileSystem.readAsStringAsync(asset.uri, { encoding: FileSystem.EncodingType.Base64 }); zip = await JSZip.loadAsync(base64, { base64: true, checkCRC32: false, createFolders: true }); }
      const next: Entry[] = []; zip.forEach((name, object) => { const extension = name.split('.').pop()?.toLowerCase() ?? ''; const metadata = object as JSZip.JSZipObject & { _data?: { uncompressedSize?: number } }; next.push({ name, size: metadata._data?.uncompressedSize ?? 0, isDirectory: object.dir, extension, kind: kindFor(extension), zipObject: object }); });
      next.sort((a, b) => Number(a.isDirectory) - Number(b.isDirectory) || a.name.localeCompare(b.name)); setEntries(next); log('Berhasil membaca ' + next.length + ' item tanpa mengekstrak seluruh ZIP'); const first = next.find(item => !item.isDirectory); if (first) await selectEntry(first);
    } catch (error) { const message = error instanceof Error ? error.message : 'Format ZIP tidak dapat dibaca'; log('ERROR ZIP: ' + message); Alert.alert('ZIP tidak terbuka', 'Arsip mungkin rusak, terenkripsi, atau memakai format kompresi yang belum didukung.'); } finally { setBusy(false); }
  };
  const selectEntry = async (entry: Entry) => {
    if (entry.isDirectory || !entry.zipObject) return; await Haptics.selectionAsync(); setSelected(entry); setPreviewText(''); setPreviewUri('');
    try {
      if (entry.kind === 'web' || entry.kind === 'code') { const text = editedContent[entry.name] ?? await entry.zipObject.async('string'); setPreviewText(text); setDraftText(text); log('Preview teks: ' + entry.name); return; }
      const base64 = await entry.zipObject.async('base64'); const safeName = entry.name.replace(/[^a-zA-Z0-9._-]/g, '_'); const uri = (FileSystem.cacheDirectory ?? FileSystem.documentDirectory ?? '') + 'zip-preview-' + Date.now() + '-' + safeName; await FileSystem.writeAsStringAsync(uri, base64, { encoding: FileSystem.EncodingType.Base64 }); setPreviewUri(uri); log('Preview aset: ' + entry.name);
    } catch (error) { log('ERROR preview ' + entry.name + ': ' + (error instanceof Error ? error.message : 'gagal membaca file')); }
  };
  const saveEdit = () => { if (!selected || selected.kind !== 'code') return; setEditedContent(current => ({ ...current, [selected.name]: draftText })); setPreviewText(draftText); setEditing(false); log('EDIT tersimpan lokal: ' + selected.name); };
  const runAudit = () => { if (!selected || !previewText) return; if (!pluginState['code-audit']) { log('PLUGIN: Code Audit sedang nonaktif.'); return; } analyzeCode(previewText, selected).forEach(log); };
  const togglePlugin = (id: string) => { setPluginState(current => ({ ...current, [id]: !current[id] })); log('PLUGIN ' + id + ': ' + (pluginState[id] ? 'nonaktif' : 'aktif')); };
  const stats = useMemo(() => ({ files: entries.filter(item => !item.isDirectory).length, media: entries.filter(item => ['image', 'audio', 'video'].includes(item.kind)).length }), [entries]);
  return <KeyboardAvoidingView style={[styles.root, { backgroundColor: colors.background, paddingTop: useSafeAreaInsets().top }]} behavior={Platform.OS === 'ios' ? 'padding' : undefined}>
    <View style={styles.header}><View><Text style={[styles.eyebrow, { color: colors.primary }]}>FROSTBYTE FILE LAB</Text><Text style={[styles.title, { color: colors.foreground }]}>ZipScope</Text><Text style={[styles.subtitle, { color: colors.mutedForeground }]}>Buka ZIP besar. Lihat, edit, dan audit isinya.</Text></View><Pressable accessibilityLabel="Pilihan plugin" onPress={() => setPluginPanel(true)} style={[styles.pluginButton, { borderColor: colors.border }]}><Ionicons name="construct-outline" size={17} color={colors.primary} /><Text style={[styles.pluginButtonText, { color: colors.primary }]}>Plugin</Text></Pressable></View>
    <Pressable testID="choose-zip" onPress={openArchive} style={({ pressed }) => [styles.openButton, { backgroundColor: colors.primary, opacity: pressed ? 0.82 : 1 }]}>{busy ? <ActivityIndicator color={colors.primaryForeground} /> : <Ionicons name="folder-open-outline" size={21} color={colors.primaryForeground} />}<Text style={[styles.openButtonText, { color: colors.primaryForeground }]}>{busy ? 'Membaca ZIP...' : 'Pilih file ZIP'}</Text></Pressable>
    <View style={styles.archiveRow}><View style={[styles.archiveIcon, { backgroundColor: colors.secondary }]}><Ionicons name="archive-outline" size={20} color={colors.primary} /></View><View style={styles.archiveMeta}><Text numberOfLines={1} style={[styles.archiveName, { color: colors.foreground }]}>{archiveName || 'Belum ada arsip dipilih'}</Text><Text style={[styles.archiveStats, { color: colors.mutedForeground }]}>{entries.length ? stats.files + ' file  ·  ' + stats.media + ' aset media' : 'Reader binary untuk ZIP besar dan folder bersarang'}</Text></View>{entries.length > 0 && <Ionicons name="checkmark-circle" size={22} color={colors.primary} />}</View>
    {!fullPreview && <View style={[styles.filePanel, { borderColor: colors.border, backgroundColor: colors.card }]}><View style={styles.panelHeading}><Text style={[styles.panelTitle, { color: colors.foreground }]}>Isi ZIP</Text><Text style={[styles.count, { color: colors.mutedForeground }]}>{entries.length}</Text></View><FlatList data={entries} keyExtractor={item => item.name} contentContainerStyle={styles.fileList} ListEmptyComponent={<View style={styles.empty}><Ionicons name="layers-outline" size={25} color={colors.mutedForeground} /><Text style={[styles.emptyTitle, { color: colors.foreground }]}>Arsip belum dibuka</Text><Text style={[styles.emptyText, { color: colors.mutedForeground }]}>Pilih ZIP untuk menampilkan semua asetnya.</Text></View>} renderItem={({ item }) => <Pressable accessibilityLabel={'Buka ' + item.name} onPress={() => selectEntry(item)} style={[styles.fileItem, selected?.name === item.name && { backgroundColor: colors.accent }]}><Ionicons name={item.isDirectory ? 'folder-outline' : iconFor(item.kind)} size={18} color={item.isDirectory ? colors.primary : colors.mutedForeground} /><View style={styles.fileText}><Text numberOfLines={1} style={[styles.fileName, { color: colors.foreground }]}>{item.name}</Text><Text style={[styles.fileSize, { color: colors.mutedForeground }]}>{item.isDirectory ? 'folder' : formatBytes(item.size)} · {item.kind}</Text></View></Pressable>} /></View>}
    <View style={[styles.stage, fullPreview && styles.stageFullscreen]}><View style={[styles.previewPanel, { borderColor: colors.border, backgroundColor: colors.card }]}><View style={styles.panelHeading}><View style={styles.panelHeadingLabel}><View style={[styles.liveDot, { backgroundColor: colors.primary }]} /><Text style={[styles.panelTitle, { color: colors.foreground }]}>{selected?.kind === 'code' ? 'Editor kode · syntax highlight' : 'Preview file dijalankan'}</Text></View><View style={styles.headerActions}>{selected?.kind === 'code' && <><Pressable accessibilityLabel="Audit kode offline" onPress={runAudit} hitSlop={8}><Ionicons name="shield-checkmark-outline" size={19} color={colors.primary} /></Pressable><Pressable accessibilityLabel="Edit file" onPress={() => { setDraftText(previewText); setEditing(true); }} hitSlop={8}><Ionicons name="create-outline" size={19} color={colors.primary} /></Pressable></>}<Pressable onPress={() => setFullPreview(value => !value)} hitSlop={10}><Ionicons name={fullPreview ? 'contract-outline' : 'expand-outline'} size={19} color={colors.mutedForeground} /></Pressable></View></View><View style={styles.previewBody}>{!selected && <View style={styles.empty}><Ionicons name="scan-outline" size={30} color={colors.mutedForeground} /><Text style={[styles.emptyTitle, { color: colors.foreground }]}>Pilih file untuk preview</Text><Text style={[styles.emptyText, { color: colors.mutedForeground }]}>HTML dijalankan dalam preview web. File kode dibaca dengan warna syntax.</Text></View>}{selected?.kind === 'web' && <WebView originWhitelist={['*']} source={{ html: previewText || '<html><body><p>Memuat preview...</p></body></html>' }} style={styles.webView} javaScriptEnabled domStorageEnabled onError={event => log('ERROR WebView: ' + event.nativeEvent.description)} onMessage={event => log('WEB: ' + event.nativeEvent.data)} />}{selected?.kind === 'image' && previewUri && <Image source={{ uri: previewUri }} resizeMode="contain" style={styles.imagePreview} />}{selected?.kind === 'audio' && previewUri && <View style={styles.mediaPreview}><Ionicons name="musical-notes-outline" size={44} color={colors.primary} /><Text style={[styles.mediaTitle, { color: colors.foreground }]}>{selected.name.split('/').pop()}</Text><AudioPlayer uri={previewUri} colors={colors} onLog={log} /></View>}{selected?.kind === 'video' && previewUri && <Video source={{ uri: previewUri }} useNativeControls resizeMode={ResizeMode.CONTAIN} style={styles.videoPreview} onError={error => log('ERROR MP4: ' + error)} />}{selected?.kind === 'code' && <ScrollView style={styles.codeScroll} contentContainerStyle={styles.codeContent}>{pluginState['syntax-highlight'] ? <HighlightedCode text={previewText || 'Memuat preview...'} extension={selected.extension} colors={colors} /> : <Text selectable style={[styles.code, { color: colors.foreground }]}>{previewText || 'Memuat preview...'}</Text>}</ScrollView>}
{selected?.kind === 'other' && <View style={styles.empty}><Ionicons name="document-text-outline" size={32} color={colors.primary} /><Text style={[styles.emptyTitle, { color: colors.foreground }]}>File siap diekspor</Text><Text style={[styles.emptyText, { color: colors.mutedForeground }]}>Format {selected.extension || 'tanpa ekstensi'} belum punya renderer internal.</Text></View>}</View></View>{!fullPreview && <View style={[styles.logPanel, { backgroundColor: colors.secondary, borderColor: colors.border }]}><View style={styles.panelHeading}><View style={styles.panelHeadingLabel}><Ionicons name="terminal-outline" size={17} color={colors.mutedForeground} /><Text style={[styles.panelTitle, { color: colors.foreground }]}>Log error & audit</Text></View><Text style={[styles.count, { color: colors.mutedForeground }]}>{logs.length}</Text></View><ScrollView style={styles.logScroll}><Text selectable style={[styles.logText, { color: colors.mutedForeground }]}>{logs.length ? logs.join('\n') : 'Tidak ada error. Jalankan Audit untuk memeriksa file kode.'}</Text></ScrollView></View>}</View><Text style={[styles.footer, { color: colors.mutedForeground }]}>{fullPreview ? 'Mode preview penuh layar' : 'Preview 70%  ·  Log 30%'}</Text>
    <Modal visible={pluginPanel} animationType="slide" onRequestClose={() => setPluginPanel(false)}><View style={[styles.pluginModal, { backgroundColor: colors.background }]}><View style={styles.modalHeader}><View><Text style={[styles.modalTitle, { color: colors.foreground }]}>Pilihan Plugin</Text><Text style={[styles.subtitle, { color: colors.mutedForeground }]}>Aktifkan fitur editor sesuai kebutuhan.</Text></View><Pressable accessibilityLabel="Tutup pilihan plugin" onPress={() => setPluginPanel(false)}><Ionicons name="close" size={26} color={colors.foreground} /></Pressable></View><ScrollView>{PLUGINS.map(plugin => <View key={plugin.id} style={[styles.pluginRow, { borderColor: colors.border }]}><View style={styles.pluginMeta}><Text style={[styles.pluginName, { color: colors.foreground }]}>{plugin.name}</Text><Text style={[styles.pluginDescription, { color: colors.mutedForeground }]}>{plugin.description}</Text><Text style={[styles.pluginBadge, { color: plugin.availableOffline ? colors.primary : '#ffbd6b' }]}>{plugin.availableOffline ? 'OFFLINE' : 'NETWORK'} · {plugin.category}</Text></View><Pressable accessibilityLabel={(pluginState[plugin.id] ? 'Nonaktifkan ' : 'Aktifkan ') + plugin.name} onPress={() => togglePlugin(plugin.id)} style={[styles.toggle, { backgroundColor: pluginState[plugin.id] ? colors.primary : colors.secondary, borderColor: colors.border }]}><Text style={{ color: pluginState[plugin.id] ? colors.primaryForeground : colors.mutedForeground, fontSize: 11, fontWeight: '700' }}>{pluginState[plugin.id] ? 'ON' : 'OFF'}</Text></Pressable></View>)}</ScrollView></View></Modal><Modal visible={editing} animationType="slide" onRequestClose={() => setEditing(false)}><KeyboardAvoidingView style={[styles.modal, { backgroundColor: colors.background }]} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><View style={styles.modalHeader}><View><Text style={[styles.modalTitle, { color: colors.foreground }]}>Edit {selected?.name}</Text><Text style={[styles.subtitle, { color: colors.mutedForeground }]}>Perubahan disimpan lokal selama sesi APK.</Text></View><Pressable accessibilityLabel="Tutup editor" onPress={() => setEditing(false)}><Ionicons name="close" size={26} color={colors.foreground} /></Pressable></View><TextInput multiline value={draftText} onChangeText={setDraftText} autoCapitalize="none" autoCorrect={false} spellCheck={false} textAlignVertical="top" style={[styles.editorInput, { color: colors.foreground, borderColor: colors.border }]} /><View style={styles.modalActions}><Pressable onPress={() => setEditing(false)} style={[styles.secondaryButton, { borderColor: colors.border }]}><Text style={{ color: colors.foreground }}>Batal</Text></Pressable><Pressable onPress={saveEdit} style={[styles.saveButton, { backgroundColor: colors.primary }]}><Ionicons name="save-outline" size={17} color={colors.primaryForeground} /><Text style={{ color: colors.primaryForeground, fontWeight: '700' }}>Simpan edit</Text></Pressable></View></KeyboardAvoidingView></Modal>
  </KeyboardAvoidingView>;
}

function AudioPlayer({ uri, colors, onLog }: { uri: string; colors: ReturnType<typeof useColors>; onLog: (message: string) => void }) { const [sound, setSound] = useState<Audio.Sound | null>(null); const [playing, setPlaying] = useState(false); const toggle = async () => { try { if (!sound) { const created = await Audio.Sound.createAsync({ uri }, { shouldPlay: true }, status => { if (status.isLoaded) setPlaying(status.isPlaying); }); setSound(created.sound); } else if (playing) await sound.pauseAsync(); else await sound.playAsync(); } catch (error) { onLog('ERROR MP3: ' + (error instanceof Error ? error.message : 'gagal memutar audio')); } }; return <Pressable onPress={toggle} style={[styles.mediaButton, { borderColor: colors.border }]}><Ionicons name={playing ? 'pause' : 'play'} size={16} color={colors.primary} /><Text style={[styles.mediaButtonText, { color: colors.primary }]}>{playing ? 'Pause audio' : 'Putar MP3'}</Text></Pressable>; }

const styles = StyleSheet.create({ root: { flex: 1, paddingHorizontal: 16 }, pluginButton: { flexDirection: 'row', alignItems: 'center', gap: 6, alignSelf: 'flex-start', paddingHorizontal: 10, paddingVertical: 8, borderWidth: 1, borderRadius: 10 }, pluginButtonText: { fontSize: 11, fontWeight: '700' }, pluginModal: { flex: 1, padding: 16, paddingTop: 48 }, pluginRow: { flexDirection: 'row', alignItems: 'center', gap: 12, paddingVertical: 13, borderBottomWidth: 1 }, pluginMeta: { flex: 1 }, pluginName: { fontSize: 14, fontWeight: '700' }, pluginDescription: { fontSize: 11, lineHeight: 16, marginTop: 3 }, pluginBadge: { fontSize: 9, fontWeight: '800', marginTop: 5, letterSpacing: 0.6 }, toggle: { minWidth: 46, height: 30, borderRadius: 15, borderWidth: 1, alignItems: 'center', justifyContent: 'center' }, header: { flexDirection: 'row', justifyContent: 'space-between', paddingTop: 16, paddingBottom: 12 }, eyebrow: { fontSize: 10, fontWeight: '700', letterSpacing: 1.7 }, title: { fontSize: 31, fontWeight: '700', letterSpacing: -1.1, marginTop: 2 }, subtitle: { fontSize: 13, marginTop: 3 }, statusDot: { width: 9, height: 9, borderRadius: 9, marginTop: 9 }, openButton: { height: 48, borderRadius: 14, alignItems: 'center', justifyContent: 'center', flexDirection: 'row', gap: 9 }, openButtonText: { fontSize: 14, fontWeight: '700' }, archiveRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 12 }, archiveIcon: { width: 38, height: 38, borderRadius: 11, alignItems: 'center', justifyContent: 'center' }, archiveMeta: { flex: 1, marginHorizontal: 11 }, archiveName: { fontSize: 14, fontWeight: '600' }, archiveStats: { fontSize: 11, marginTop: 3 }, filePanel: { height: 145, borderWidth: 1, borderRadius: 16, overflow: 'hidden', marginBottom: 10 }, panelHeading: { minHeight: 42, paddingHorizontal: 13, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, panelHeadingLabel: { flexDirection: 'row', alignItems: 'center', gap: 7 }, headerActions: { flexDirection: 'row', alignItems: 'center', gap: 14 }, panelTitle: { fontSize: 12, fontWeight: '700', letterSpacing: 0.2 }, count: { fontSize: 11 }, fileList: { paddingHorizontal: 7, paddingBottom: 8 }, fileItem: { minHeight: 46, borderRadius: 10, paddingHorizontal: 9, flexDirection: 'row', alignItems: 'center', gap: 9 }, fileText: { flex: 1 }, fileName: { fontSize: 12, fontWeight: '600' }, fileSize: { fontSize: 10, marginTop: 3 }, stage: { flex: 1, minHeight: 250, flexDirection: 'row', gap: 10 }, stageFullscreen: { marginHorizontal: -16 }, previewPanel: { flex: 7, borderWidth: 1, borderRadius: 16, overflow: 'hidden' }, logPanel: { flex: 3, borderWidth: 1, borderRadius: 16, overflow: 'hidden' }, previewBody: { flex: 1, padding: 10 }, empty: { flex: 1, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 20, gap: 7 }, emptyTitle: { fontSize: 14, fontWeight: '700', textAlign: 'center' }, emptyText: { fontSize: 12, lineHeight: 17, textAlign: 'center' }, liveDot: { width: 7, height: 7, borderRadius: 7 }, webView: { flex: 1, backgroundColor: '#fff' }, imagePreview: { flex: 1, width: '100%', height: '100%' }, videoPreview: { flex: 1, width: '100%', height: '100%' }, codeScroll: { flex: 1, borderRadius: 10, backgroundColor: '#06101d' }, codeContent: { padding: 12 }, code: { fontFamily: 'monospace', fontSize: 11, lineHeight: 18 }, lineNumber: { color: '#52647a' }, logScroll: { flex: 1, paddingHorizontal: 12, paddingBottom: 10 }, logText: { fontFamily: 'monospace', fontSize: 10, lineHeight: 16 }, mediaPreview: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 10 }, mediaTitle: { fontSize: 14, fontWeight: '700', textAlign: 'center' }, mediaButton: { flexDirection: 'row', alignItems: 'center', gap: 7, paddingVertical: 10, paddingHorizontal: 13, borderWidth: 1, borderRadius: 10 }, mediaButtonText: { fontSize: 12, fontWeight: '700' }, footer: { fontSize: 10, textAlign: 'center', paddingTop: 8, paddingBottom: 10 }, modal: { flex: 1, padding: 16, paddingTop: 48 }, modalHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 14 }, modalTitle: { fontSize: 19, fontWeight: '700', maxWidth: '85%' }, editorInput: { flex: 1, borderWidth: 1, borderRadius: 12, padding: 12, fontFamily: 'monospace', fontSize: 13, lineHeight: 20, backgroundColor: '#06101d' }, modalActions: { flexDirection: 'row', justifyContent: 'flex-end', gap: 10, paddingTop: 12 }, secondaryButton: { minHeight: 44, paddingHorizontal: 16, borderWidth: 1, borderRadius: 10, alignItems: 'center', justifyContent: 'center' }, saveButton: { minHeight: 44, paddingHorizontal: 16, borderRadius: 10, flexDirection: 'row', gap: 7, alignItems: 'center', justifyContent: 'center' } });
